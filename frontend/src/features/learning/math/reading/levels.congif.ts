export type ReadingLevel = {
  level: number;
  title: string;
  description: string;
};

const mathReadingLevels: ReadingLevel[] = [
  {
    level: 1,
    title: "Number Recognition",
    description: "Recognize and read numbers and basic math symbols."
  }
];

export default mathReadingLevels;
